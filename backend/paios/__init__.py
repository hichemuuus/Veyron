"""PAIOS — Personal AI Operating System.

A local agent runtime: an AI Core (agent + planner + memory + tasks), a
sandboxed tool system, and a mission-control web UI. See ARCHITECTURE.md.
"""

__version__ = "0.1.0"
